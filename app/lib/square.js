"use strict";

class Square {
  constructor(size, color, offset, index) {
    this.size = size; // <Size>
    this.color = color; // <Color>
    this.offset = offset; // <Point>
    this.index = index // <Number>
  }
}

module.exports = Square
